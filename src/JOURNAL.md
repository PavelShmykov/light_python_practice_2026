# Журнал работ

## 06.06.2026 — Этап 1

**Что сделано:**
- создан main.py с функцией `main()`
- создан db_sqlite3.py с функциями `init_db()` и `demo_db()`
- база данных создаётся без ошибок
- заполнен student_meta.md
- добавлен README.md

**Что не получилось:**
- ошибок нет

**Следующий шаг:**
- этап 2: рекурсивный обход папки и сохранение файлов в БД

## 07.06.2026 — Этап 2

**Что сделано:**
- создан filter.py с функцией `is_allowed()`
- создан scanner.py с функциями `scan_folder()` и `scan_recursive()`
- в модуль db_sqlite3 добавлена функция `save_files()`

**Что не получилось:**
- ошибок нет

**Следующий шаг:**
- функция для сохранения в БД истории запусков

## 08.06.2026 — Этап 2

**Что сделано:**
- добавлена в модуль db_sqlite3 функция `save_scan_run()`

**Что не получилось:**
- ошибок нет

**Следующий шаг:**
- этап 3: посчитать хэш файлов и найти повторяющиеся

## 09.06.2026 — Этап 3

**Что сделано:**
- создан `hasher.py` с функцией `get_hash()` — подсчёт md5 хэша файла
- в `db_sqlite3.py` добавлена функция `add_hash_column()` — колонка hash в таблице files
- в `db_sqlite3.py` добавлена функция `save_hash()` — сохранение хэша в базу
- в `db_sqlite3.py` добавлена функция `get_duplicates()` — поиск файлов с одинаковым хэшем

**Что не получилось:**
- ошибок не было

**Следующий шаг:**
- этап 4: сравнение папки с бэкапом и вывод различий

## 10.06.2026 — Этап 4

**Что сделано:**
- создан `backup.py` с функцией `compare_folders()` — сравнение папки с бэкапом
- в `db_sqlite3.py` добавлена функция `init_backup_table()` — создание таблицы backup_checks
- в `db_sqlite3.py` добавлена функция `save_backup_check()` — сохранение результатов проверки
- в `db_sqlite3.py` добавлена функция `print_table()` — универсальный вывод таблицы из БД

**Что не получилось:**
- ошибок не было

**Следующий шаг:**
- практика завершена, все 4 этапа сданы

## 26.06.2026 — Этап 4

**Что сделано:**
- аргументы теперь передаются через терминал при помощи библиотеки argparse